# Wrangler configuration for Historical Data Worker

name = "coinswarm-historical-data"
main = "historical-data-worker.ts"
compatibility_date = "2024-01-01"

# KV Namespace for historical price data
# Created via GitHub Actions workflow
# ID will be auto-assigned on first deployment or fetched from dashboard
[[kv_namespaces]]
binding = "HISTORICAL_PRICES"
# Note: ID will be populated after first KV namespace creation
# Check GitHub Actions logs or Cloudflare dashboard for the namespace ID

# Environment variables
[vars]
# Binance API base URL (public, no auth needed for historical data)
BINANCE_API_URL = "https://api.binance.com"
